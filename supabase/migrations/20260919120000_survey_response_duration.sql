-- مدتِ زمانِ پاسخ‌دهی (بر حسبِ ثانیه) — از لحظه‌ای که پاسخ‌دهنده وارد سؤال‌ها
-- می‌شود (یا برای آزمونِ زمان‌دار، از زدنِ «شروعِ آزمون») تا لحظه‌ی ثبتِ نهایی.
-- سمتِ کلاینت محاسبه و همراهِ پاسخ‌ها به submit-survey-response فرستاده
-- می‌شود؛ رکوردهای قبل از این تغییر مقدارِ NULL دارند (مدت‌شان نامشخص است).
alter table survey_responses
  add column if not exists duration_seconds integer;
